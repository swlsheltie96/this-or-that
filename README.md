# elochamber2

Normally

```
bun install
bun --watch server.ts
```

When running tests or load testing

```
BENCHMARK=1 bun --watch server.ts
```

To run a test:

```
bun tests/test.ts
```

To run a load test:

```
bun tests/load.ts
```
